# Node.js Hello World Application - Environment Variables
# Copy this file to '.env' and modify the values as needed

# The TCP port number on which the HTTP server will listen for incoming connections
# Default: 3000
# Valid values: Number between 1024-65535
PORT=3000

# The Node.js environment mode which can affect logging and error handling behavior
# Default: development
# Valid values: development, production, test
NODE_ENV=development